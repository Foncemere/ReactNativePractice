import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  primaryContainer: {
    minWidth: 350,
    alignItems: 'center',
    backgroundColor: '#F55772',
    padding: 20,
    margin: 10,
    borderRadius: 50,
  },
});

export default Styles;
